spring:
  cloud:
    config:
      name: feign
      uri: http://localhost:8089/ #指明配置服务中心的网址。
      profile: dev
      label: master #配置仓库的分支

      #通过eureka获取配置服务中心
#      discovery:
#        enabled: true
#        serviceId: config-server

